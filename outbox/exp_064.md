# exp_064 — Entropy Annealing

**Result: FAILURE** — 7.78 mean reward (flat), 0 gates, 0.52s deterministic / 0.88s stochastic

- Removed max_logstd clamp, raised ent_coef 0.01→0.03 with annealing to 0.001, doubled budget
- Reward never broke out of ~8 in 2.5M steps (exp_060 reached 28 with clamp + lower ent_coef)
- Annealing schedule too slow: only reached 6.3% of planned iterations, ent_coef still ~0.028 at end
- Three-way confound: cannot separate clamp removal, ent_coef increase, and budget effects
- Suggested next: isolate clamp removal by running exp_060 config with only max_logstd removed
